{
  "name": "visitor-management-system",
  "version": "1.0.0",
  "description": "A production-ready, secure corporate visitor management system featuring dynamic webcam captures, instant QR badge generation, thermal card printing templates, real-time analytics widgets, and a dual-storage adapter (Google Sheets + Google Drive integration with automatic local filesystem fallbacks).",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd frontend && npm install && npm run build && cd ../backend && npm install",
    "start": "node backend/src/server.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs"
}
